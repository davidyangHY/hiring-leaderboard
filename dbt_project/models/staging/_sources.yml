version: 2

sources:
  - name: raw
    description: Tables written outside dbt by src/snapshot.py.
    schema: main
    tables:
      - name: role_snapshots
        description: One row per (captured_at, company, role_id) — daily board snapshots.
      - name: companies
        description: Tracked-company dimension mirrored from seed/companies.csv.
